{
  "name": "csv-importer-crm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build --workspaces",
    "start": "concurrently \"npm run start -w frontend\" \"npm run start -w backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
